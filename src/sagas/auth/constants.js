export const reducerName = 'auth';

export const GET_JWT = `${reducerName}:get_jwt`;
export const VALIDATE_JWT = `${reducerName}:validate_jwt`;
export const REFRESH_JWT = `${reducerName}:refresh_jwt`;
export const DESTROY_JWT = `${reducerName}:destroy_jwt`;
